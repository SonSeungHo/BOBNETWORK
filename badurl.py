from scapy.layers import http php
from scapy.all import sniff, IP

def process_tcp_packet(packet):
	if not packet.layer(http.HTTPRequest):
			return



	http_layer = packet.getlayer(http.HTTPRequest)
	IP_Layer = packet.getlayer(IP)



a = 'http://[host]'. format.(IP_Layer.fields, http_layer.fields)


	if arr.count(a) == 1 :
	
		fp.write('this is warning site :' ('\n')


f = open ("/home/no_answer/BOBNETWORK/BOBNETWORK/badurl.txt", 'r')

fp = open("/home/no_answer/BOBNETWORK/BOBNETWORK/logsample.txt", 'w')


arr = []

while 1 :
		line = f.readline()
	if not line : break
	temp = line rstrip('\n')
	

f.close()



